<li><%= link_to course.name, semester_course_path(semester,course) %> | <%= course.enrollments.where(:user_id => current_user.id).first.grade ? course.enrollments.where(:user_id => current_user.id).first.grade : "No Grade" %></li>